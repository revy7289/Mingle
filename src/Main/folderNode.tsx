export default function TEST_FOLDER({ children }) {
    return (
        <div className="w-[300px] h-[200px] rounded-2xl flex justify-center items-center bg-white">
            {children}
        </div>
    );
}
